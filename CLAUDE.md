# boudreaux — Claude Code

Read [~/.config/agents/AGENTS.md](~/.config/agents/AGENTS.md) before doing
anything — it governs how to work overall, interact, and manage sessions.

@AGENTS.md

Load lessons (`docs/lessons/<category>/`) and nested directory `AGENTS.md`
files only when the work matches them, per the tables above — recurse into
category directories when you load one. When corrected or when you catch your
own mistake, record the lesson as a file in the matching category before
continuing.
